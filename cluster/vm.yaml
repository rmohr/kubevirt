apiVersion: kubevirt.io/v1alpha1
kind: VirtualMachine
metadata:
  name: testvm29
spec:
  domain:
    devices:
      graphics:
      - type: spice
      interfaces:
      - type: nodeNetwork
        model:
          type: virtio
        mac:
          address: 6e:3f:a2:cf:f8:12
      video:
      - type: qxl
      disks:
      - type: network
        snapshot: external
        device: disk
        driver:
          name: qemu
          type: raw
          cache: none
        source:
          host:
            name: iscsi-demo-target.default
            port: "3260"
          protocol: iscsi
          name: iqn.2017-01.io.kubevirt:sn.42/2
        target:
          dev: vda
      consoles:
      - type: pty
    memory:
      unit: MB
      value: 64
    os:
      type:
        os: hvm
    type: qemu
